/**
 * CerberusStyle is a type that defines the style properties that can be applied to a component.
 * All the numeric values are considered as pixels.
 */

export type CerberusStyle = React.CSSProperties;
